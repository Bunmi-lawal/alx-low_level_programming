#include "3-calc.h"
/**
 * op_add - sums a and b
 * @a: first num
 * @b: second num
 * return: sum of two numbers
 */
int op_add(int a, int b)
{
return (a + b);
}
/**
 * op_sub - subtract a from b
 * @a : first num
 * @b : second num
 * return: result of subtracted number
 */
int op_add(int a, int b)
{
return (a - b)
}
/**
 * op_mul - multipy a by b
 * @a: first num
 * @b: second num
 * return: result of mutiplication
 */
int op_mul(int a, int b)
{
return (a * b);
}
/**
 * op_div - divide a by b
 * @a : first num
 * @b : second num
 * return : divided result
 */
int op_div(int a, int b)
{
return (a / b);
}
/**
 * op_mod - modulation of a and b
 * @a : first num
 * @b : second num
 * return : result of modulation
 */
int op_mod(int a, int b)
{
return (a % b);
}
