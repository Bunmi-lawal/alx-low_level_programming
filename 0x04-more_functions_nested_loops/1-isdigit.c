#include "main.h"
/**
 * _isdigit - checks if c is a digit
 * @c - character to be tested
 * return - 1 if c is a digit, o otherwise
 */
int _isdigit(int c);
{
if ((c >= 48) && (c <= 57))
{
return (1);
}
return (0);
}
