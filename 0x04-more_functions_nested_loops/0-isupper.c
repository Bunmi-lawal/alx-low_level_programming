#include "main.h"
/**
 * _isupper - check if character is upper or lowercase
 * @c: character to be tested
 * Return: 1 or 0 if otherwise
 */
int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))
{
return (1);
return (0);
}
