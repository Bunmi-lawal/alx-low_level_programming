#include "main.h"
/**
 * _abs - prints the absolute value of an integer
 * @b: number targeted
 * Return: returns 0
 */
int _abs(int b)
{
if (b > 0)
return (b);
else if (b < 0)
return (-b);
else
return (0);
}
