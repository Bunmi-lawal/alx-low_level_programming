#include <stdio.h>
/**
 * puts_half - function that prints half of a string
 * @str : string to look at
 * return : void
 */
void puts_half(char *str)
{
int i = 0;
int bun;
while (str[i] != '\0')
{
j++;
}
if (1 % 2 == 1)
{
bun = (i - 1) / 2;
bun += 1;
}
else
{
bun = i / 2;
}
for (; bun < i; bun++)
{
putchar(str[bun]);
}
{
putchar('\n');
}
}
