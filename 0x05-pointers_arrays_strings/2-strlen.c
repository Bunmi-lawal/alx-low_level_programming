#include <stdio.h>
#include <string.h>
/**
* _strlen - function that returns the length of a string
* @s : srting that counts
* Return : length  of string
*/
int _strlen(char *s)
{
int b = 0;
(b = 0; *s != '\0'; s++);
{
b++;
}
return (b);
}
